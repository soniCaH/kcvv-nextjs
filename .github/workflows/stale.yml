# This workflow warns and then closes issues and PRs that have had no activity for a specified amount of time.
#
# You can adjust the behavior by modifying this file.
# For more information, see:
# https://github.com/actions/stale
name: Mark stale issues and pull requests

on:
  schedule:
    - cron: "39 23 * * *"

jobs:
  stale:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    steps:
      - uses: actions/stale@4391f3da665fdf50b6810c1a66712fb9ba21aa93 # v11.0.0
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          stale-issue-message: "This issue has had no activity for a while and will be closed soon if it remains inactive. Comment to keep it open."
          stale-pr-message: "This pull request has had no activity for a while and will be closed soon if it remains inactive. Comment to keep it open."
          stale-issue-label: "no-issue-activity"
          stale-pr-label: "no-pr-activity"
