#!/usr/bin/env bash
echo "🔍 Running pre-commit checks..."

# Run lint-staged for staged files
npx lint-staged

# Type check
echo "📝 Type checking..."
npm run type-check

# Run tests only for changed files (much faster!)
echo "🧪 Running tests for changed files..."
npm run test:changed -- --silent --reporter=dot

echo "✅ Pre-commit checks passed!"
